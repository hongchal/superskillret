---
name: My Bad Name!!
description: This fixture uses a name with uppercase letters, spaces, and punctuation so the validator rejects it for not matching the kebab-case rule.
---

# Bad name

Body text long enough to clear the minimum-length rule so the only failure
is the name shape. The validator should report the regex it expects.
